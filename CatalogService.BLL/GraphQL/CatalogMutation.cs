namespace CatalogService.BLL.GraphQL
{
    public class CatalogMutation
    {
        //[Error(typeof(ArgumentException))]
        //public async Task<Category> AddCategory([Service] ICatalogService catalog, CategoryDTO category)
        //{
        //    var newCat = await catalog.AddCategory(category);
        //    return await catalog.GetCategory(newCat.Id);
        //}
    }
}
